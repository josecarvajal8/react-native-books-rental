import React, { ReactNode, useEffect } from 'react';
import 'react-native-gesture-handler';
import Root from './Root';
const App: () => ReactNode = () => {
  return (
    <Root />
  );
}

export default App;
